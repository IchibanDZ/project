<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <div class="d-flex flex-column">
      <a class="navbar-brand" href="/">MyLOC</a>
        {% if is_granted('ROLE_ADMIN') %}
          <p class="bg-danger text-light text-end px-2 fs-6 fw-bold">ADMIN</p>
        {% elseif app.user %}
          <p class="bg-primary text-light text-end px-2 fs-6 fw-bold">{{app.user.firstName}}</p>
      {% endif %}
      
    </div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/tools">Outils</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#"  role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Catégories
          </a>
        <ul class="dropdown-menu">
    {% for category in categories %}
        <li><a class="dropdown-item" href="/categories/{{ category.id }}">{{ category.name }}</a></li>
    {% endfor %}
</ul>

        </li>
        {% if is_granted('ROLE_ADMIN') %}
          <li class="nav-item">
            <a href="/admin" class="btn btn-danger">Admin</a>            
          </li>
        {% elseif app.user %}
          <li class="nav-item">
            <a href="/tools/add" class="btn btn-primary">Ajouter un outil + </a>            
          </li>
        {% endif %}
        {% if app.user %}
          <li class="nav-item">
            <a href="/logout" class="btn btn-warning mt-2">Se déconnecter</a>
          </li>
        {% else %}
          <li class="nav-item">
            <a href="/login" class="btn btn-primary mt-2">Se Connecter</a>
          </li>
          <li class="nav-item">
            <a href="/register" class="btn btn-primary mt-2">Créer un Compte</a>
          </li>
        {% endif %}
      </ul>
      <form class="d-flex">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>
